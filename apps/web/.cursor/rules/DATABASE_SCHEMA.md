-- WARNING: This file is a POINTER only — not a full schema dump.
--
-- Canonical monorepo database schema reference:
--   docs/DATABASE_SCHEMA.sql
--
-- Policy and sync rules (issue #25):
--   docs/DATABASE_SCHEMA.md
--
-- Do not maintain a second full dump here; it will diverge from the CMS and
-- from production. Open the canonical SQL file when you need columns/constraints.
--
-- Active AOP entity: public.aop (int4). Legacy uuid public.appellations is backup only.
-- Recent focused fields: aop.recognition_year; wine_region_history_milestones.
